import React from "react";

const StartPage = () => {
  return (
    <div
      style={{
        height: "200rem",
        width: "100%",
        display: "flex",
        justifyContent: "center",
      }}
    >
    </div>
  );
};

export default StartPage;
